#include<bits/stdc++.h>
using namespace std;
/*
	-			 Algorithm Library 
	- cac tao tac tren thu vien thuat toan deu su dung tham so la dia chi ( con tro )
	- truy xuat ptu : {Tên_mang} + {Vi_trí}; // doi voi mang
	-				{Tên_vector}.begin() + {Vi_trí};


	-	max(a, b) ,	min(a, b); // Hàm tìm min , max  giua a và b.
	- 	*min_element(l, r); 	// Tìm min doan . L va R la 2 con tro-> chi vao dia chi dau va cuoi
	-	sort(l, r);  // sap xep thu tu tang dan . mac dinh cua sort la less ( tang dan )
	- sort(l, r, greater / less < {Kieu phan tu} >()); // thuat toan sap xep tang dan hoac giam dan 
		ham se tra ve kq trong doan    L -> R - 1 

	- 		swap({Bien_thu_nhat}, {Bien_thu_hai}) // hoan doi gia tri

		
	- Linenear Search : tim kiem ptu trong mang  
	- Pair < int, string > student;
		{Tên_bien_pair}.first
		{Tên_bien_pair}.second
		pair < int, string > student;
		cout << student.first << ' ' << student.second;



*/
int main(){
	
	
	return 0;
}
